# Flask Web Framework and Server
Flask==2.3.3
gunicorn==21.2.0

# Celery and Redis for Background Tasks
celery==5.3.6
redis==5.0.1

# Cloud Provider SDKs
oci==2.125.1
azure-identity==1.15.0
azure-mgmt-compute==30.1.0
azure-mgmt-network==25.3.0
azure-mgmt-resource==23.1.0
boto3==1.34.8

# Core Dependencies
cryptography==41.0.7  # 锁定一个与各SDK兼容的版本
certifi==2024.7.4     # 锁定我们验证过的稳定版本
requests
